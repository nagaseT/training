<!DOCTYPE html>
<head>
  <meta charset="utf-8">
  <title>main</title>
</head>
<body>
  <h1>Main</h1>
  <h3>Login User</h3>
    <p><%= loginUser %></p>
  <h3>User List</h3>
  <ul id="user_list">
    <% users.forEach(function(user) { %>
      <li><a href="localhost:3000/user?username=<%= user.username %>"><%= user.username %></a></li>
    <% }) %>
  </ul>
</body>
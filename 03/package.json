{
  "name": "login",
  "version": "0.0.0",
  "description": "login server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "body-parser": "^1.13.1",
    "ejs": "^2.3.1",
    "express": "^4.13.0"
  },
  "devDependencies": {
    "mocha": "^2.2.5"
  }
}
